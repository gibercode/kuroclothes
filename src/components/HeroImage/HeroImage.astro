---
import styles from "./style.module.scss";
---

<script>
  const arrow = document.querySelector<HTMLElement>("#arrow");
  arrow?.addEventListener("click", () => {
    const sections = document.querySelectorAll<HTMLElement>(
      ".products-container"
    );
    sections.forEach((item: HTMLElement) => {
      item.scrollIntoView({ behavior: "smooth" });
    });
  });

  const button = document.querySelector<HTMLElement>("#product-btn");
  button?.addEventListener("click", () => {
    const sections = document.querySelectorAll<HTMLElement>(
      ".products-container"
    );
    sections.forEach((item: HTMLElement) => {
      item.scrollIntoView({ behavior: "smooth" });
    });
  });
</script>

<div class={styles.main}>
  <div class={styles.mainContainer}>
    <div style={styles.container}>
      <div class={styles.titleContainer}>
        <h1 class={styles.title}>KURO</h1>
        <h1 class={styles.titlevariant}>CLOTHES</h1>
      </div>
      <h3 class={styles.subtitle}>クロの服</h3>

      <button class={styles.button} id="product-btn">Ver productos</button>
    </div>

    <div class={styles.icon_container} id="arrow">
      <img
        class={styles.arrow_icon}
        src="/down-arrow.png"
        width={20}
        alt="arrow-icon"
        loading="lazy"
        decoding="async"
      />
    </div>
  </div>
</div>
